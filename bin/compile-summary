#!/bin/bash

set -o errexit
set -o nounset


name="$(basename $(pwd))"

# Check for pre-process stuff.
if [[ -f Makefile ]]; then
    make preprocess
fi

# Compile
latexmk -pdf -shell-escape "$name-summary.tex"
